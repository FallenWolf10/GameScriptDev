version: 1
name: Invalid Bid King Unsupported Action

target:
  window_title_contains: SAFE PLACEHOLDER

window:
  resolution:
    width: 1
    height: 1
    policy: ignore

execution:
  max_retries: 1
  allow_infinite_run: false

initial_state: home
states:
  home:
    actions:
      - type: click_magic_button
    on_success: done
    on_failure: done
  done:
    terminal: true
    result: invalid_example_done
