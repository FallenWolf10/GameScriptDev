# Python runner with dry-run first

The runner will be implemented in Python and default to dry-run execution. Python gives the project a practical path to YAML loading, desktop automation, screenshots, and template matching, while dry-run-first execution lets profiles and state graphs be validated before the tool is allowed to send live mouse or keyboard input.
