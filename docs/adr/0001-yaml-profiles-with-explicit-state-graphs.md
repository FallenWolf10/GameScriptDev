# YAML profiles with explicit state graphs

Game profiles will be declarative YAML files with strict schema validation and explicit transitions between states. This favors human-editable workflow files, readable comments, preflight graph validation, and clearer failure logs over shorter but more ambiguous inferred transitions or game-specific code profiles.
