# Separate Flow View from State View

The Profile Builder will use a two-level central workspace: Flow View presents a compact State and Transition graph, while State View presents the selected State's anchors and ordered Scratch-like Actions. The palette, target preview, inspector, selection, and navigation context remain persistent across both views; this avoids an unscalable canvas containing every State and Action while keeping authoring within one Builder screen.
